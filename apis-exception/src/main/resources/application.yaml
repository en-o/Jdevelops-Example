spring:
  profiles:
    active: dev
#logging:
#  level:
#    root: error
#  org.springframework.boot: debug



---
spring:
  config:
    activate:
      on-profile: dev
jdevelops:
  exception:
    log-input: false
    http-servlet-response-status: true

---
spring:
  config:
    activate:
      on-profile: prod
jdevelops:
  exception:
    enabled: false

